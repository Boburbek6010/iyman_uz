import 'dart:ui';

class AppColors {

  // splash page color
  static const backgroundColor = Color(0xFFffffff);
  static const appTextColor = Color(0xFF549830);
  
  // intro page color 
 static const backgroundColorIntroPage = Color(0xFFffffff);
 static const introPageButtonColor = Color.fromRGBO(95, 165, 238, 1);

 // indicator color
 static const indicatorColor = Color.fromRGBO(20, 20, 20, 1);

 // namoz screen color
 static const namozScreenAppBarColor = Color.fromRGBO(47, 93, 40, 1);
 static const iconColor = Color.fromRGBO(20, 20, 20, 1);
}